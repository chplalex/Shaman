<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_shaman"
        android:roundIcon="@drawable/ic_shaman"
        android:supportsRtl="true">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
            < Search Framework в коробке не умеет работать с Fragment
            < при запросе создается новый Activity, а это некрасиво
            < буду дорабатывать
            <
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
            -->
        </activity>

        <provider
            android:name=".Start.FragmentStartSuggestionProvider"
            android:authorities="com.example.myapp.Start.FragmentStartSuggestionProvider"></provider>

        <service android:name=".MyFirebaseMsgService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>